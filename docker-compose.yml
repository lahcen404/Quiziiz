services:
  quiziiz-frontend:
    build: .
    container_name: quiziiz_app
    ports:
      - "8080:80" # Maps your computer's port 8080 to Docker's port 80
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - quiziiz-network

networks:
  quiziiz-network:
    driver: bridge