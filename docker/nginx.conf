server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This line is the magic: it redirects 404s to index.html for Vue Router
        try_files $uri $uri/ /index.html;
    }
}